# React Vite Dashboard

A modern React dashboard built with Vite. This project demonstrates a structured React setup with reusable components, portfolio data, and dashboard sections.

## Run locally

1. Open a terminal in `react-dashboard`
2. Run `npm install`
3. Run `npm run dev`
4. Open the local URL shown in the terminal
